name: Test Fibbot
on: [push, pull_request]
jobs:
  run-fibbot:
  runs-on: ubuntu-latest
  steps:
    - name: Checkout code
    uses: actions/checkout@v2
    - name: Set up Rust
    uses: dtolnay/rust-toolchain@stable
    - name: Build and run
    run: cargo run
